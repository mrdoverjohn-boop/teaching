# PUSH THIS INTO THE TEACHING REPO

Extract this zip.

Copy the `CHALLENGES` folder into the root of your teaching repo.

Then from the repo:

```bash
git add CHALLENGES
git commit -m "Add 50 programming challenges"
git push
```
